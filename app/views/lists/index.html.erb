<h1>My Lists</h1>

<% @lists.each do |list| %>
    <div>
        <p><%= link_to list.title, list_path(list) %>(<%= link_to "Edit", edit_list_path %>)</p>
    </div>
<% end %>

<p><%= link_to "New List", new_list_path %> </p>